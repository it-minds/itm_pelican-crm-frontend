import React, { FC } from 'react';

type LocationFilterProps = {
	locations: string[];
};

const LocationFilter: FC<LocationFilterProps> = ({ locations }) => {
	return (
		<></>
		// <PopupFilterWrapper>
		// 	<CheckboxGroup />
		// </PopupFilterWrapper>
		// 	<Box display="flex" width="100%" justifyContent="space-between" mt="1rem">
		// 	<Button variant="contained" sx={{ width: '4.2rem' }}>
		// 		<Typography variant="body2">None</Typography>
		// 	</Button>
		// 	<Button variant="contained" sx={{ width: '4.2rem' }}>
		// 		<Typography variant="body2">All</Typography>
		// 	</Button>
		// </Box>
	);
};

export default LocationFilter;

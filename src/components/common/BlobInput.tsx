import { ButtonBase } from '@mui/material';
import React, { FC } from 'react';

type BlobInputProps = {};

const BlobInput: FC<BlobInputProps> = ({}) => {
	return (
		<div>
			<ButtonBase>Test</ButtonBase>
		</div>
	);
};

export default BlobInput;
